import React from "react";

/** @type {{[class: string]: React.CSSProperties}} */
const styles = {
    wrapper: {
        
    }
};

/**
 * 
 * @param {{
 * actions: {label: string; action: () => void}[];
 * style: React.CSSProperties;
 * }} param0 
 */
export default function SwiperAction ({actions, style}) {

}